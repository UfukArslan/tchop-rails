<%= form_with(model: city) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= form.fields_for :restaurants do |restaurants_form| %>
        <%= restaurants_form.label :name %>
        <%= restaurants_form.text_field :name %>

        <%= restaurants_form.label :description %>
        <%= restaurants_form.text_field :description %>
    <% end %>
  </div>


  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :npa %>
    <%= form.text_field :npa %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
